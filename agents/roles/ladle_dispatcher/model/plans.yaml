ladleAssignmentPlan:
  stepSchema:
    service:
      type: string
    args:
      type: object
  allowedServices:
    - createDispatchTask
    - lockLadle
    - assignLadleToConverter
    - triggerReplan
  successCondition: "this.variables.lastError == ''"
